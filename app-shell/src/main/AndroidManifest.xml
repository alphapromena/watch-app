<?xml version="1.0" encoding="utf-8"?>
<!--
  Scaffold-only manifest. The full permission set, foreground-service
  declaration, and activity wiring is added in a follow-up commit
  (see ongoing-scope item #1 in the stream brief).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="WatchApp"
        android:supportsRtl="true" />

</manifest>
