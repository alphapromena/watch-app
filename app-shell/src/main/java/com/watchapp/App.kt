package com.watchapp

import android.app.Application

/**
 * Process-wide entry point. The [com.watchapp.di.Container] is constructed
 * here in onCreate() in a follow-up commit.
 */
class App : Application()
